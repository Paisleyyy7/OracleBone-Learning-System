<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.SessionMapper">

    <update id="updateById" parameterType="com.example.entity.Session">
        update session
        <set>
            <if test="userID != null">userID = #{userID},</if>
            <if test="startTime != null">startTime = #{startTime},</if>
        </set>
        where sessionID = #{sessionID}
    </update>

    <insert id="insert" parameterType="com.example.entity.Session" useGeneratedKeys="true">
        insert into session
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="sessionID != null">sessionID,</if>
            <if test="userID != null">userID,</if>
            <if test="startTime != null">startTime,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="sessionID != null">#{sessionID},</if>
            <if test="userID != null">#{userID},</if>
            <if test="startTime != null">#{startTime},</if>
        </trim>
    </insert>

    <select id="selectByUserId" resultType="com.example.entity.Session">
        select *
        from session
        where userID = #{userID}
    </select>

</mapper>
